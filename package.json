{
  "name": "global-crisis-observatory",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "precheck": "npm run typecheck",
    "check": "npm test",
    "postcheck": "npm run build"
  },
  "devDependencies": {
    "@types/node": "latest",
    "jsdom": "^29.1.1",
    "typescript": "latest",
    "vite": "^8.0.16",
    "vitest": "latest"
  }
}
